use serde::Deserialize;

use crate::cli::Args;
use crate::gui::draw_state::DrawMode;
use crate::gui::dsel_state::DSelMode;
use crate::gui::map::{MapEditMode, RoomId, RoomMap};

pub fn convert_0_1(args: Args) {
    todo!()
}
